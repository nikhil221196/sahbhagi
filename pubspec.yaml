
name: institute_chat_app
description: A chat app for the institute with Firebase

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.4.1
  firebase_auth: ^4.2.0
  cloud_firestore: ^4.2.0
  firebase_storage: ^11.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
